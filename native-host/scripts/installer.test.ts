import {readFile} from "node:fs/promises";import {describe,expect,it} from "vitest";
describe("instalación Native Messaging",()=>{it("Setup espera confirmación y desinstala las claves",async()=>{const source=await readFile("native-host/installer.nsh","utf8");expect(source).not.toContain("register --all");expect(source).not.toContain("customInstall");expect(source).toContain("Google\\Chrome");expect(source).toContain("Microsoft\\Edge");});it("portable no ejecuta registro",async()=>{const pkg=JSON.parse(await readFile("package.json","utf8"));expect(pkg.build.portable).toBeDefined();expect(pkg.build.portable).not.toHaveProperty("include");});});
