export type Platform='twitch'|'kick'; export type MonitorStatus='off'|'starting'|'checking'|'active'|'paused'|'partial-error';
export interface Streamer{id:string;platform:Platform;displayName:string;normalizedName:string;externalId?:string;url:string;avatar?:string;enabled:boolean;live:boolean;title?:string;category?:string;lastCheckedAt?:string;openedAt?:string;sessionId?:string;openedSessionId?:string;lastError?:string}
export interface Settings{scanMinutes:number;idleMinutes:number;autoStart:boolean;countdownSeconds:number;startup:boolean;startMinimized:boolean;minimizeToTray:boolean;notifications:boolean;browserMode:'default'|'managed';closeManagedTabs:boolean;theme:'light'|'dark'|'system';supportText:string;supportCount:number;showStartNotice:boolean;platforms:Record<Platform,{enabled:boolean;clientId?:string;accessToken?:string}>}
export interface Activity{id:string;at:string;level:'info'|'warning'|'error';platform?:Platform;channel?:string;description:string}
export interface AppState{schemaVersion:number;settings:Settings;streamers:Streamer[];activity:Activity[];monitor:{status:MonitorStatus;lastScan?:string;nextScan?:string;errors:string[]}}
export const defaults:AppState={schemaVersion:1,settings:{scanMinutes:15,idleMinutes:10,autoStart:true,countdownSeconds:30,startup:false,startMinimized:false,minimizeToTray:true,notifications:true,browserMode:'default',closeManagedTabs:true,theme:'system',supportText:'HeyGuys',supportCount:3,showStartNotice:true,platforms:{twitch:{enabled:false},kick:{enabled:false}}},streamers:[],activity:[],monitor:{status:'off',errors:[]}};
