import {z} from 'zod';
const platform=z.enum(['twitch','kick']); const streamer=z.object({id:z.string().uuid(),platform,displayName:z.string().min(1).max(60),normalizedName:z.string().regex(/^[a-z0-9_]{2,30}$/),externalId:z.string().max(64).optional(),url:z.string().url().refine(v=>/^https:\/\/(www\.)?(twitch\.tv|kick\.com)\/[a-z0-9_]+$/i.test(v)),avatar:z.string().url().optional(),enabled:z.boolean(),live:z.boolean(),title:z.string().max(300).optional(),category:z.string().max(100).optional()}).strip();
export function parseImport(raw:string){if(new TextEncoder().encode(raw).length>1024*1024)throw new Error('El archivo supera 1 MB.');const parsed=JSON.parse(raw);return z.object({streamers:z.array(streamer).max(500),settings:z.record(z.unknown()).optional()}).parse(parsed)}
