declare const chrome:any; const status=document.querySelector("#status")!; const detail=document.querySelector("#detail")!; chrome.runtime.sendMessage({action:"status"}).then((v:any)=>{status.textContent=v?.connected?"Aplicación conectada":"Aplicación desconectada";detail.textContent=v?.connected?`Pestañas administradas: ${v.managedTabs}. Versión ${v.version}.`:"Abre Apoya a tu Streamer para activar la extensión.";}).catch(()=>{status.textContent="Aplicación desconectada";});
