import{describe,expect,it}from'vitest';import{TWITCH_SCOPES,TwitchApiError}from'./twitch-auth';describe('OAuth Twitch',()=>{it('usa permisos mínimos de chat para bot',()=>expect(TWITCH_SCOPES).toEqual(['user:write:chat','user:bot']));it('modela token caducado',()=>expect(new TwitchApiError(401,'caducado').status).toBe(401));it('modela rate limit',()=>expect(new TwitchApiError(429,'limitado',30).retryAfter).toBe(30))});
